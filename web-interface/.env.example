# Local default — keep this for npm run dev on your PC.
VITE_API_BASE_URL=http://localhost:5050/api/v1

# Azure / production example (rebuild frontend after changing):
# VITE_API_BASE_URL=https://YOUR-BFF-APP.azurewebsites.net/api/v1
