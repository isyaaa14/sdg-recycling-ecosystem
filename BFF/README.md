# SDG BFF Server

Backend-for-Frontend (Express) for the web portal in `web-interface/`.

## Run locally
```bash
npm install
npm start
```

Default port: 5050. Copy env from Azure / local `.env` (do not commit secrets).
